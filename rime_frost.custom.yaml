patch:
  speller/algebra:
    - xform/^([aoe])(ng)?$/$1$1$2/
    - xform/^([aoe])([ioun])$/$1$1$2/
    - xform/^([jqxy])u$/$1v/
    - derive/ong$/on/
    - derive/on$/ong/
    - derive/([zcs])h/$1/
    - derive/([zcs])([^h])/$1h$2/

    - derive/([ei])n$/$1ng/   # in→ing, en→eng
    - derive/([ei])ng$/$1n/   # ing→in, eng→en

    - erase/^xx$/
